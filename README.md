# Automated Web Crawling Using Selenium

### Setup:

```
git clone https://github.com/GinoP123/Web-Crawler.git
cd Web-Crawler

conda create -f env.yml
conda activate webscrape
npm install ttab -g
python -m ipykernel install --user --name webscrape

./shell.sh
```